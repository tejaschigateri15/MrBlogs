<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MR. Blogs | <%= title %></title>
    <link rel="icon" type="image/x-icon" href="/icons8-favicon-64.png">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">

    <style>
        
        @import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&display=swap');
        @import url('https://fonts.googleapis.com/css2?family=Raleway:wght@200&display=swap');
        @import url('https://fonts.googleapis.com/css2?family=Noto+Sans:wght@600&display=swap');
        @import url('https://fonts.googleapis.com/css2?family=DM+Sans:opsz,wght@9..40,300&display=swap');
        .title{
            padding-left: 35px;
            color: darkslategray;
            margin-bottom: 20px;
            

        }
        .head{
            margin: auto;
            max-width: fit-content;
            padding-top: 15px;
            padding-left: 10px;
            text-align: center;
            
        }

        .head{-webkit-animation:head .8s cubic-bezier(.215,.61,.355,1.000) both;animation:head .8s cubic-bezier(.215,.61,.355,1.000) both}

        @-webkit-keyframes head{0%{letter-spacing:1em;opacity:0}40%{opacity:.6}100%{letter-spacing:normal;opacity:1}}@keyframes head{0%{letter-spacing:1em;opacity:0}40%{opacity:.6}100%{letter-spacing:normal;opacity:1}}
        .head{
            padding-top: 80px;
        }
       
        .snippet{
            padding-left: 36px;
            margin-bottom: 15px;
            padding-right: 20px;
           
            
        }
        .body{
            padding-left: 36px;
           
            margin-right: 35px;
            border-left: 5px solid crimson;
           
        }
        .blogscontent {
        margin-top: 150px;
        background-color: white;
         padding: 20px;
        
         max-width: 1050px; /* Adjust the maximum width as needed */
         margin: 0 auto; /* Center the box horizontally */
        
         
  }
  .separator {
    margin: 30px 0;
    border: none;
    
}
.blog-post {
    background-color: rgb(245, 245, 245);
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 4px 12px rgba(142, 142, 140, 0.781);
    margin-bottom: 20px;
}
    .blog-container{
        margin-top: 30px;
    }


  
    </style>
</head>
<body>
    
    <%- include('./nav.ejs')  %>
    <%- include('./try.ejs')  %>

    <div class="blogscontent">
        <h2 class="head"><%= head %></h2>
        <% if (blogs.length > 0) { %>
        <% blogs.forEach(blog => { %>
        <div class="blog-container">
            <div class="blog-post">
                <h2 class="title"><%= blog.title %></h2>
                <p class="snippet"><b><%= blog.snippet %></b></p>
                <p class="body"><%= blog.body %></p>
            </div>
            <div class="separator"> <!-- Add a separator between blog posts  All Blogs  --></div>
        </div>
        <% }) %>
        <% } else { %>
            <p>There are no blogs created right now...</p>
        <% } %>
    </div>
    
    


    <%- include('./footer.ejs')  %>

</body>
</html>